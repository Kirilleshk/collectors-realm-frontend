# Collector's Realm — Claude Code Context

Мобильное приложение для коллекционеров фигурок на React Native/Expo.

## Стандарты работы Claude

- **Код:** писать как лучший программист — ставить каждое решение под сомнение, проверять на баги минимум 3 раза перед выводом, всегда думать как сделать лучше.
- **Ответы:** перед финальным ответом на любую задачу — пересмотреть его 3 раза, убедиться что решение оптимальное, поставить под сомнение и улучшить.
- **После завершения работы за сессию** — заносить в этот файл (раздел «Задачи клиента» / соответствующий раздел) краткую запись о том, что было сделано: что именно, в каких файлах, дата.
- **Изменения кода** — после каждого `git push` сразу (не откладывая до конца сессии) дописывать краткий итог в соответствующий раздел этого файла.
- **Важная инфа от пользователя** (пароли, ключи, доступы, решения, договорённости) — если пользователь дал что-то, что стоит помнить в будущих сессиях, спросить, нужно ли занести это в CLAUDE.md (или в память), а не просто использовать один раз.

## Запуск проекта

```powershell
cd C:\Users\ksele\collectors-realm
npx expo start --web --clear
```

## Технический стек

- **Фронтенд:** React Native + Expo SDK 54
- **Бэкенд:** Node.js + TypeScript + Prisma + PostgreSQL (GitHub → Render.com, auto-deploy)
- **БД:** PostgreSQL на **Supabase** (eu-west-1 Ireland, Free план, бессрочно)
- **Фото:** Cloudinary (unsigned upload)
- **Навигация:** React Navigation (Stack + Bottom Tabs)

## Важные константы

```javascript
API           = 'https://collectors-realm-backend.onrender.com/api'
CLOUD_NAME    = 'dqutmb1rm'
UPLOAD_PRESET = 'collectors_realm'   // unsigned
PROJECT_ID    = 'ee592544-47bd-4d06-8f93-0070a93efe36'
EXPO_ACCOUNT  = 'kirill24125'
SERVICE_ID    = 'srv-d7hlnhfaqgkc739da4p0'  // Render сервис
```

## Тестовые аккаунты

```
Обычный      : kirill@test.com  / password123   (роль COLLECTOR)
Админ (тест) : admin@test.com   / admin123        (26.07.2026: логин вернул "Пользователь не найден" в проде — похоже, не заведён/не существует в текущей БД Supabase)
Админ (реал) : ksele52@gmail.com / см. приватную память Claude (НЕ в этом файле — репозиторий публичный на GitHub)
```

Доступ к вкладке «Админ»: `user?.roles?.includes('ADMIN'|'ANALYTICS'|'MODERATOR')` (`App.js`) — проверка по роли из JWT, не по email. (Уточнено 25.08.2026 — раньше здесь была устаревшая запись про проверку по подстроке email, в актуальном коде её уже нет.) Бэкенд отдельно проверяет ту же роль для стафф-эндпоинтов (`/api/cards/admin/*` и т.п.).

## Структура фронтенда

```
collectors-realm/
├── App.js                   ← Навигация (ShopStack, MapStack, MainTabs)
├── app.json                 ← Конфиг Expo + EAS
├── eas.json                 ← Конфиг сборки Android/iOS
└── src/
    ├── api.js               ← axios + auth/products/wishlist/users
    ├── AuthContext.js       ← user, token, login, register, logout, updateUser
    ├── notifications.js     ← Expo Push Notifications
    ├── theme.js             ← bg, surface, surface2, text, text2, accent, border, blue, purple, green
    └── screens/
        ├── LoginScreen.js          ← Вход + регистрация (4 роли-карточки)
        ├── ShopScreen.js           ← Магазин: фото, поиск, фильтры
        ├── ProductDetailScreen.js  ← Карточка товара, галерея, Telegram/WhatsApp/Max
        ├── AdminScreen.js          ← CRUD товаров, смена статуса
        ├── ProfileScreen.js        ← Профиль: аватар, роли, геолокация, портфолио
        ├── UserProfileScreen.js    ← Публичный профиль пользователя
        ├── MapScreen.js            ← Leaflet (iframe веб / WebView мобайл), фильтры ролей
        ├── WishlistScreen.js       ← Вишлист с приоритетами
        ├── ChatScreen.js           ← Чат с продавцом
        ├── NotificationsScreen.js  ← Уведомления
        ├── CollectionScreen.js     ← Моя коллекция
        └── ReleasesScreen.js       ← Анонсы релизов
    └── utils/
        ├── uploadPhoto.js          ← Загрузка фото (web + mobile)
        ├── SmartInput.js           ← TextInput с автоскроллом на web
        ├── analytics.js            ← track() — отправка событий аналитики
        ├── WhatsNewModal.js        ← Модал "Что нового" при обновлении
        └── changelog.js            ← История версий (ОБНОВЛЯТЬ ПРИ КАЖДОМ РЕЛИЗЕ)
```

## Навигация (App.js)

```
RootNav
├── Login (не авторизован)
└── Main → MainTabs
    ├── Магазин → ShopStack (ShopList → ProductDetail → Chat / Notifications / UserProfile)
    ├── Карта  → MapStack (MapMain → UserProfileMap)
    ├── Вишлист
    ├── Админ
    └── Профиль
```

## Роли пользователей

```
COLLECTOR     → Коллекционер      🗿  синий
MASTER_REPAIR → Мастер по ремонту 🔧  accent/оранжевый
CUSTOMIZER    → Кастомизатор      🎨  фиолетовый
DIORAMA       → Мастер диорам     🏔  зелёный
ADMIN         → полная админка (ksele52@gmail.com)
ANALYTICS     → только статистика (для клиента-заказчика)
MODERATOR     → ограниченная админка для Марка: вкладки «Люди» (блокировка),
                «Чат» (поддержка + удаление сообщений), «Статистика».
                Без доступа к «Товары»/«Релизы» (зона Кирилла)
```

## Статусы товаров

```
AVAILABLE  → Доступен      зелёный  #34C759
SOLD       → Продан        серый    #8E8E93
PREORDER   → Предзаказ     синий    #007AFF
RESERVED   → Резерв        оранжевый #FF9500
NEGOTIABLE → Торг уместен  фиолетовый #AF52DE
```

## API эндпоинты

### Авторизация
```
POST /api/auth/login     { email, password } → { token, user }
POST /api/auth/register  { name, email, password, roles[] } → { token, user }
```

### Товары
```
GET    /api/products
GET    /api/products/:id
POST   /api/products           { ...fields, imageUrls[] }
PUT    /api/products/:id
PATCH  /api/products/:id/status  { status }
DELETE /api/products/:id
```

### Пользователи
```
GET  /api/users           все пользователи (карта)
GET  /api/users/me        мой профиль (include portfolioPhotos)
GET  /api/users/:id       публичный профиль
PUT  /api/users/me        { name, city, bio, roles, latitude, longitude, avatarUrl }
POST /api/users/me/avatar
POST /api/users/me/fcm-token
```

### Вишлист
```
GET    /api/wishlist
POST   /api/wishlist        { name, priority, comment }
PUT    /api/wishlist/:id
DELETE /api/wishlist/:id
```

## Схема БД (Prisma)

```prisma
enum UserRole      { COLLECTOR, MASTER_REPAIR, CUSTOMIZER, DIORAMA, ADMIN, ANALYTICS }
enum Condition     { NEW, USED }
enum ProductStatus { AVAILABLE, SOLD, PREORDER, RESERVED, NEGOTIABLE }
enum Priority      { HIGH, MEDIUM, LOW }

model User {
  id, email, phone, passwordHash, name, city
  latitude, longitude, roles[], collectorGrade
  bio, avatarUrl, fcmToken
  wishlist WishlistItem[]
  portfolioPhotos PortfolioPhoto[]
}
model Product {
  id, name, description, price, condition
  status ProductStatus @default(AVAILABLE)
  manufacturer, franchise, character
  images ProductImage[]
}
model ProductImage   { id, url, productId, order }
model WishlistItem   { id, userId, name, priority, comment }
model PortfolioPhoto { id, url, userId, order }
```

## Загрузка фото (Cloudinary)

```javascript
const fd = new FormData()
fd.append('file', { uri: result.assets[0].uri, type: 'image/jpeg', name: 'photo.jpg' })
fd.append('upload_preset', 'collectors_realm')
const r = await fetch('https://api.cloudinary.com/v1_1/dqutmb1rm/image/upload', { method: 'POST', body: fd })
const d = await r.json()
// d.secure_url — итоговый URL
```

## Авторизация

```javascript
// AsyncStorage хранит токен и данные пользователя
// AuthContext: user, token, loading, login, register, logout, updateUser
// updateUser(data) — обновляет user в памяти и AsyncStorage без запроса к серверу
```

## Важные заметки

1. **Бэкенд на GitHub** — изменения через GitHub, Render деплоит автоматически при push в main.
2. **Миграции БД** — создавать в `prisma/migrations/` на GitHub, файл `migration.sql`.
3. **Статусы PREORDER/RESERVED/NEGOTIABLE** — добавлены в schema.prisma, могут не применились в БД — нужна SQL миграция.
4. **Карта** — Leaflet через iframe (веб) и WebView (мобайл). Маркеры кликабельны → карточка пользователя.
5. **Push-уведомления** — не работают в Expo Go, нужен development build.

## Известные проблемы

- Render Free: первый запрос 50+ сек (засыпает)
- react-native-webview 13.16.1 вместо 13.15.0 — не критично
- ✅ Веб-версия: карточки магазина растянуты — исправлено 01.07.2026,
  сетка колонок теперь зависит от ширины экрана (`ShopScreen.js`)

## Задачи клиента (приоритет по порядку)

1. ✅ **Клавиатура загораживает экран** — KeyboardAvoidingView во всех экранах с вводом
2. ✅ **Кнопки телефона пересекаются** — SafeAreaView + useSafeAreaInsets в tabBarStyle
3. ✅ **Статус товара не меняется на Android** — исправлен products.routes.ts + SQL миграция enum
4. ✅ **Обязательное фото при регистрации** — двухшаговая регистрация, мин. 1 фото
5. ✅ **Расширить вишлист** — поля: originalName, characterRu, manufacturer, releaseDate
6. ✅ **Коллекционеры поблизости** — кнопки 5км/20км в MapScreen, Haversine, кружок радиуса
7. ✅ **Аукцион** — таймер, ставки, бейдж АУКЦИОН, бэкенд endpoints
8. ⏳ **Вход через ВКонтакте** — нужен VK App ID с dev.vk.com

## Задачи клиента Марка (по памяти Claude)

1-3. ✅ Внешние ссылки убраны, канал сменён на markeltoys, кнопка TG у Админа убрана
4. ✅ Внутренний чат «Связь с администрацией» (профиль ↔ AdminScreen → вкладка «Чат»)
5. ✅ Расширенная статистика — фронтенд готов (новые за неделю, среднее время,
   популярные разделы); бэкенд требует мерджа `backend-analytics-extra.ts`
6. ✅ Второй админ-аккаунт (роль `MODERATOR`) — блокировка, удаление сообщений,
   кнопка связи у заблокированных. Требует: миграции `MODERATOR` в enum UserRole,
   применения `backend-set-mark-role.sql` (сменить роль аккаунта Марка с ADMIN на MODERATOR)
7. ⏳ Карточная игра «Карты Средиземья» — геймификация, отдельная большая фича
8. 🔄 Библиотека знаний гик-культуры — MVP готов (24.07), формат статьи
   доработан под образец Марка 01.08 (подзаголовки, картинка персонажа из
   ru.wikipedia, курируемый список ~240 персонажей). Заблокировано дальнейшее
   масштабирование дневным лимитом токенов Groq — нужно решение пользователя
   (отдельный ключ / подождать / снизить длину статьи)

**Чат «Связаться с продавцом»** теперь реальный (раньше был локальный мок):
переиспользует `SupportMessage` с привязкой к товару (`productId`/`productName`),
все обращения попадают в единый тред пользователя с админом/модератором.

## Итоги сессий

**26.08.2026 (2)** — найден и исправлен баг «4-й/5-й слот на столе боя обрезаются» (заметка `cmt9xhwc7`, "уже почти месяц"):
- ✅ **Root cause найден точным замером реального DOM на проде** (не
  догадка): у `boardSlots=5` ряд слотов не помещается в одну строку по
  ширине на портретных экранах (~390px) — переносится на 3+2
  (`flexWrap:'wrap'`). Формула высоты слота (`BattleScreen.js`) всегда
  делила высоту арены на 2 (boss-ряд + ряд игрока), как будто каждая
  сторона — ровно одна строка. Реально нужно 2 подстроки на сторону (4
  суммарно). Т.к. у арены `overflow:'hidden'`, "лишние" подстроки — то
  есть 4-й и 5-й слот игрока — обрезались **полностью** (замерено:
  99px, весь слот целиком, а не край картинки). Раньше ошибочно считал,
  что это про обрезку самой картинки карты (resizeMode) — нет, слоты
  целиком уезжали за пределы видимой области контейнера.
- ✅ Фикс: `arenaWidth` теперь измеряется тем же `onLayout`, что и
  `arenaHeight` (тот же принцип "считать по факту, не гадать", что уже
  применялся раньше). По ширине считается, сколько слотов реально
  помещается в строку (`slotsPerLine`), отсюда `linesPerSide` — и высота
  слота делится на `linesPerSide * 2`, а не на жёсткую `2`. В ландшафте
  (compact) 5 слотов обычно помещаются в одну строку по ширине — там
  перенос не нужен, слоты не мельчают зря; в портрете корректно
  закладываются 2 строки на сторону.
- ⚠️ **Не проверено вживую до конца** — сборка (Metro) прошла без ошибок,
  формула пересчитана и перепроверена вручную по реальным DOM-размерам,
  снятым на проде в этой же сессии ДО фикса (`arenaHeight=381,
  arenaWidth=390` → новый `slotSize=71px`, бюджет высоты сходится с
  запасом ~3px) — но инструмент браузерной проверки (Playwright)
  отключился в процессе сессии и не переподключился, живого скриншота
  ПОСЛЕ фикса нет. Заметка бота оставлена `IN_PROGRESS`, не закрыта
  `DONE` — по правилу проекта не закрывать без реальной проверки.
  **Следующий шаг новой сессии**: перепроверить вживую на проде (5
  существ на столе игрока одновременно, портретный экран ~390px) и
  только тогда закрыть заметку `cmt9xhwc7`.
- Коммит `c2e4344`, задеплоено на Cloudflare.

**26.08.2026** — срочная задача Марка (лимит фото) + случайно найденный 404 при F5:
- Проверил тг-бот на новые заметки — нашлось 4 (все сегодня, 10:05-10:08):
  срочно поднять лимит фото при регистрации с 5 до 10 (сделано, см. ниже),
  портфолио-карусель для мастера по ремонту (не начато, отдельная фича),
  повторная жалоба на обрезку карт на СТОЛЕ боя ("уже почти месяц", не
  начато — это BoardSlot, другой компонент, чем HandCard, который чинил
  вчера), и мета-просьба Марка писать отчёты понятнее/с самопроверкой —
  учту.
- ✅ **Лимит фото в портфолио 5 → 10** (при регистрации и в профиле) —
  бэкенд не ограничивал число фото вообще (`portfolioUrls: z.array` без
  `max` в `users.routes.ts`), ограничение было чисто на фронтенде.
  Вынесено в константу `MAX_PORTFOLIO_PHOTOS` в `LoginScreen.js` и
  `ProfileScreen.js` (обе версии редактирования + счётчик + подсказка).
  Минимум 3 фото при регистрации не менялся. Проверено локально
  (`(0/10)` в профиле), задеплоено.
- ✅ **Побочно найден и исправлен отдельный настоящий баг** при деплое —
  прямая ссылка на любой экран, кроме главного (и обновление страницы
  F5, если пользователь не на нём) отдавала чистый **404** от Cloudflare
  вместо приложения: статик-ассеты не знали о клиентском роутинге React
  Navigation. Добавлен `not_found_handling: "single-page-application"`
  в `wrangler.jsonc` (отдаёт `index.html` 200 на любой путь) — стандартный
  SPA-фоллбэк. Проверено `curl` до/после (404→200) и вживую в браузере.
  Не путать со вчерашней находкой (кнопка «назад» браузера не создаёт
  историю для вложенных экранов) — это разные баги на разных уровнях
  (сервер отдачи статики vs клиентский роутинг), сегодняшний фикс НЕ
  решает вчерашнюю проблему с «назад».
- Коммиты `e747120` (лимит фото), `5dc9513` (SPA-фоллбэк), задеплоено
  на Cloudflare (`npm run deploy`, первая попытка упала по сетевому
  таймауту к Cloudflare API — как и раньше в проекте, помог повтор
  `npx wrangler deploy`).

**25.08.2026 (3)** — полная QA-проверка приложения (по прямой просьбе пользователя):
- Прошёл вживую (Playwright, прод) через Магазин/товар/чат/Библиотеку/Карту/
  Моё (Коллекция+Вишлист)/Профиль. 0 ошибок в консоли на всех экранах.
- ✅ **Починен реальный оверфлоу-баг**: попап пользователя на карте
  (`MapScreen.web.js`) — `truncateBio` (введён 19.08) режет bio по числу
  символов, но без `word-break` длинное "слово" без пробелов (вставленная
  ссылка и т.п.) всё равно рисуется одной сплошной строкой далеко за
  пределы карты — у Leaflet-попапа нет доступа к RN `numberOfLines`.
  Обёрнул содержимое попапа в `<div style={{maxWidth, overflowWrap:
  'anywhere', wordBreak: 'break-word'}}>`. Проверено локально до/после
  на том же тестовом аккаунте с патологическим bio, задеплоено.
- ⚠️ **Найден, но НЕ исправлен более крупный баг навигации**: переход
  на вложенный экран (карточка товара, чат, профиль пользователя и
  т.п.) не создаёт запись в истории браузера (`window.history.length`
  подтверждённо не меняется при переходе Shop→ProductDetail) —
  `NavigationContainer` в `App.js` без явного `linking` конфига не
  синхронизирует URL/историю для вложенных стеков (только вкладки
  верхнего уровня получают свой путь). Следствие: физическая кнопка/жест
  «назад» браузера или телефона с ЛЮБОГО вложенного экрана не
  возвращает на экран назад внутри приложения, а закрывает всё
  приложение целиком (уходит на `about:blank`/предыдущий сайт).
  Внутренняя стрелка "←" в шапке работает нормально — баг только у
  системной кнопки/жеста "назад". Полноценный фикс — добавить `linking`
  конфиг с путями для вложенных экранов — трогает навигацию всего
  приложения, не стал делать без явного решения пользователя (в проекте
  уже был случай дорогого отката большой недосогласованной фичи, см.
  «20.07.2026 (4)»). Ждёт решения — чинить сейчас или отложить.
- Мелкое (не баг, тестовые данные): описание товара "Deadpool" в БД
  буквально `"11"` — старая тестовая запись, не код.
- Отдельно (не баг, документация): актуальный код (`App.js:90`) даёт
  доступ к вкладке «Админ» по роли (`ADMIN`/`ANALYTICS`/`MODERATOR`) из
  JWT, а не по подстроке email, как было ранее записано в этом файле —
  запись поправлена (см. выше в разделе «Тестовые аккаунты»). Также
  замечено: модалка "Что нового" на входе показывает явно устаревший
  список (v1.2.0, темы из старых сессий) — `src/utils/changelog.js`
  давно не обновлялся вопреки правилу проекта, отдельно не трогал.

**25.08.2026 (2)** — честный отчёт по библиотеке знаний (заметка `cmsm3t3qi`, 09.08):
- Марк просил отчёт "как закончишь все 240 персонажей". Вместо того чтобы
  гадать по памяти — добавил в бэкенд read-only staff-эндпоинт
  `GET /api/library/admin-coverage` (сверяет `docs/library-characters-list.txt`
  с уже закешированными `LibraryArticle`, ни одного обращения к Groq, квоту
  не тратит), задеплоил, запросил через реальный админ-аккаунт.
- **Честный результат — работа НЕ закончена**: 109 из 242 (45%), 133 ещё
  отсутствуют, у 40 из готовых нет картинки. По правилу проекта (не
  закрывать DONE без реальной проверки) — отправил Марку именно этот
  промежуточный статус, а не "готово", объяснил бутылочное горлышко
  (дневной лимит токенов Groq, общий с расшифровкой голосовых бота и
  живым поиском пользователей — самозаливка сама ждёт лимит и продолжает
  на следующий день). Заметка переведена `NEW` → `IN_PROGRESS` (не
  закрыта).
- ✅ Заодно нашёл через новый эндпоинт (`suspiciouslyShort`) старый
  известный баг: статья "Гамора" оборвалась посреди предложения ещё
  01.08 (задокументировано в сессии 01.08, тогда не долечили — упёрлись в
  квоту) и с тех пор так и осталась битой. Попытка перегенерировать
  вручную (`?refresh=1`) тоже упёрлась в дневной лимит (502) — квота
  на сегодня уже исчерпана. **Починил саму причину, почему баг не
  исчезал сам**: `libraryAutoSeed.ts` считал персонажа готовым по одному
  факту наличия строки в БД, без проверки качества контента — битый
  слаг никогда бы не попал в повторную генерацию. Теперь автозаливка
  также проверяет длину статьи (`< 400` символов = брак) и лечит такие
  случаи наравне с полностью отсутствующими персонажами.
- Коммиты `8852bf3` (эндпоинт), `99ab83f` (самолечение автозаливки),
  Render автодеплой.

**25.08.2026** — найден и исправлен реальный баг «карты обрезаются» (жалоба Марка от 19.08):
- Начал сессию по правилу из точки возврата (см. «21.08.2026 (3)» ниже) —
  запросил `GET /api/mark-bot/notes`. Новых заметок после 21.08 не было, но
  нашлось **4 заметки от 19.08, оставшиеся необработанными** (`NEW`,
  видимо пришли уже после того, как сессия 19.08 закрыла свою часть
  бэклога): мета-комментарий Марка про нехватку времени + пояснение идеи
  лестницы боссов от нейросети (`cmszxf2ve`), референс-картинка
  вертикальной карты-пути уровней (`cmszxf8za`, тот самый вариант, что
  предлагался на выбор 21.08), общая жалоба на библиотеку знаний +
  **резкая жалоба, что карты в игре обрезаются "уже раз пять чинили и
  нихера не исправили"** (`cmszxfbg2`). Показал сводку пользователю,
  спросил приоритет — выбрана жалоба на карты.
- ✅ **Баг найден и подтверждён живьём** (Playwright на проде, 844×390 —
  компактный ландшафтный режим боя): это НЕ регрессия фиксов от 09-10.08
  (`resizeMode: contain` на месте, не откачен) — отдельный, более старый
  баг переноса текста. Длинные имена карт ("Опустошитель", "Преторианец",
  "Сокольник") в компакт-режиме не помещались даже в 2 строки при узком
  слоте 46px — вместо аккуратного multi-line ellipsis браузер обрубал
  вторую строку посередине глифов (подтверждено замером через
  `getBoundingClientRect`/`scrollHeight` в реальном браузере — контент
  физически требовал 3 строки, а `numberOfLines={2}` показывал только
  «голову» второй строки без чистого «…»). В портретном (некомпакт)
  режиме бага нет — там карты крупнее, имена помещаются в 1-2 строки без
  переполнения.
- ✅ Фикс: `HandCard.js` — новый проп `nameLines` (по умолчанию 2),
  `BattleScreen.js` передаёт `nameLines={1}` только в компакт-режиме —
  на 1 строке `text-overflow: ellipsis` работает надёжно в любом браузере
  (карта показывает «Опус…», полное имя — по долгому нажатию через уже
  существующий `CardZoomModal`). Портретный режим не тронут (там `nameLines`
  остался 2, как раньше).
- Проверено локальным dev-сервером (`npx expo start --web`) до деплоя —
  сравнил через `sharp` увеличенные кропы карты до/после фикса (наглядно
  видно обрубленные буквы → чистое «Сок…»), затем то же самое ещё раз
  живьём на проде после деплоя. Коммит `90aa8a6`, `npm run deploy` на
  Cloudflare. Заметка бота `cmszxfbg2` закрыта `DONE` с summary — Марку
  ушло уведомление с точным объяснением находки.
- Остальные 3 заметки от 19.08 и более старый бэклог (батч про статьи
  библиотеки от 29.07, отчёт по 240 персонажам, карта-путь уровней) —
  **не тронуты в этой сессии**, ждут своей очереди по приоритету
  пользователя.

**21.08.2026 (3)** — точка возврата для продолжения в новом чате:
- После лестницы боссов (см. «21.08.2026» ниже) и фиксов iPhone/логина
  (см. «21.08.2026 (2)» ниже) пользователь спросил "что дальше" — дал на
  выбор: автогенерация картинок через Leonardo.ai / красивая карта-путь
  уровней вместо простого списка / вернуться к тг-боту задач Марка / своё.
- **Пользователь выбрал: вернуться к боту задач.** Сам запрос ещё НЕ
  выполнен — начал вызывать `GET /api/mark-bot/notes`, но пользователь
  прервал вызов инструмента и попросил сначала записать состояние сюда
  (этот файл), чтобы можно было продолжить в другом чате с этого же места.
- **How to apply — первое действие в новой сессии**: выполнить
  `GET /api/mark-bot/notes` (заголовок `x-bot-secret`, см.
  [[reference_mark_telegram_bot]] в памяти Claude), найти заметки новее
  последней уже разобранной (см. «21.08.2026 (2)» — там разобраны 2
  заметки от 21.08 про iPhone/логин, они последние известные), разобрать
  всё новое по обычному правилу (не закрывать `DONE` без реальной проверки
  на проде). Никакого другого невыполненного технического долга на конец
  этой сессии не осталось — лестница боссов и iPhone/логин фиксы уже
  закоммичены и задеплоены (детали ниже).

**21.08.2026 (2)** — 2 новых бага от Марка (вход не сохраняется + не открывается на iPhone):
- ✅ Проверил тг-бота на новые заметки — нашлось 2 свежих (сегодня, 21.08):
  «на айфоне не открывается по основной ссылке» и «вход не сохраняется, если
  не заходил день-два — просит войти заново». Разобрал их сразу, не откладывая.
- Разобрался в причине бага с выходом из аккаунта: JWT-токен и так живёт
  90 дней (поднято 09.08), `AuthContext.js`/`api.js` читают/пишут его
  правильно (проверил код, устаревших ключей хранения в истории нет) —
  значит дело не в токене, а в том, что сам браузер стирает `localStorage`.
  Приложение — чистый веб без единой настройки для «Добавить на экран
  Домой» (`public/index.html` не имел даже `manifest.json`/apple-иконки) —
  ссылку каждый раз открывают заново в Safari или во встроенном браузере
  Telegram/WhatsApp, а оба варианта на iOS известны агрессивной очисткой
  хранилища через несколько дней без визита сайта.
- ✅ Добавлены `public/manifest.json` + apple-mobile-web-app-*/theme-color
  теги в `index.html`, иконка `public/icon-512.png` — теперь можно закрепить
  приложение на экране домой полноценной иконкой, у такой Safari хранит
  логин надёжнее, чем у обычной вкладки. Попутно поправлены 2 устаревших
  комментария «токен живёт 7 дней» (было исправлено на 90 ещё 09.08, но
  комментарии не обновили). Проверено на проде напрямую (curl) — оба файла
  отдаются 200, новые теги в финальном HTML.
- ⚠️ **Не решает проблему полностью** — это смягчение поведения iOS, а не
  фикс бага в коде (баг не найден, потому что его и нет — это ограничение
  платформы без нативного приложения). Настоящее решение то же самое, что
  уже в бэклоге — приложение в сторе.
- Баг «не открывается на iPhone» технически не смог диагностировать без
  ответа Марка — коду тут почти неоткуда взяться (ссылка одна для всех
  платформ, отдельных ссылок под Android/iOS не существует и не создавал,
  раз он попросил именно это) — по факту, скорее всего, это уже известная
  блокировка `workers.dev` Роскомнадзором (см. [[project_workers_dev_blocked_russia]],
  не переименовано без решения Марка о покупке домена). Отправил Марку
  через бота диагноз обоих багов + 2 уточняющих вопроса (Safari или
  in-app-браузер мессенджера; был ли включён VPN) — без ответа точную
  причину №1 не подтвердить.
- Обе заметки бота оставлены `IN_PROGRESS` (не `DONE`) — реального
  подтверждения фикса от Марка ещё нет, только смягчение с моей стороны.
  Коммит `3b1826f`, задеплоено (Cloudflare `npm run deploy`).

**21.08.2026** — PvE-лестница боссов (первый шаг направления, подтверждённого 19.08):
- ✅ По итогам скоупинга через Plan Mode + Explore-агент (полный разбор
  боевого движка перед стартом — см. память Claude `project_mark_tasks`)
  реализована лестница из **4 боссов** внутри темы «Чужой против Хищника»:
  Boss #1 = существующая «Королева чужих» (баланс не тронут), + 3 новых
  босса с эскалацией HP (45/60/81) и уникальной пассивкой у каждого —
  `regen` (лечение в начале хода), `mana_drain` (меньше маны игроку),
  `enrage` (бонус урона ниже 50% HP). Следующий босс открывается только
  победой над предыдущим (`UserBossProgress`), проверено что поражение
  анлок не даёт. Арт новых боссов — временная заглушка (портрет «Королевы
  чужих»), по решению пользователя не блокировать механику ожиданием арта.
- Новый экран `LevelSelectScreen.js` — простой список (карта-путь в стиле
  референса Марка — отдельный шаг позже, после проверки механики), между
  коллекцией и боем. Пассивка босса видна игроку и в списке уровней, и в
  бою (`BossBanner`) — не скрытая механика.
- Попутно почищен старый мёртвый код: `BattleScreen.js` игнорировал
  `route.params` целиком (всегда резюмировал "текущий" бой), `startBattle`
  принимал неиспользуемый `themeId`.
- Backend: новые модели `Boss`/`UserBossProgress`, самоприменяющийся
  сид-скрипт (без ручного SQL), новый эндпоинт `GET /api/cards/bosses`.
  Проверено end-to-end через автоматизированный плейтест (curl/node) —
  победа над боссом действительно открывает следующего, `regen` лечит
  ровно на заявленное значение и не превышает потолок HP, попытка начать
  заблокированного босса напрямую по API отклоняется 403 с понятной
  ошибкой. UI подтверждён вживую (Playwright) — список уровней с
  правильными иконками ✅/🔒/›, 0 ошибок в консоли.
- **Отдельно всплыла идея** пользователя — научить Claude самому
  генерировать картинки через API Leonardo.ai (тот же сервис, которым
  Марк уже пользуется вручную). Технически реально при наличии ключа с
  существующего аккаунта, сознательно отложено пользователем до
  завершения этой фичи.

**19.08.2026** — проверка бота на новые задачи + картинки библиотеки (реальные люди-тёзки):
- Проверил тг-бот Марка на новые заметки с 12.08 — нашлось 6: реальный
  новый баг картинок библиотеки (15.08, со скриншотами), 4 заметки про
  контент-бота канала Markeltoys (16.08, статус поправлен `NEW`→
  `IN_PROGRESS` — это продолжение уже известного отдельного проекта, не
  новая задача, сам проект на паузе по слову Марка). Заодно поправил
  статус большого опроса от 09.08 (`NEW`→`IN_PROGRESS` — часть пунктов
  внутри реально сделана, часть нет).
- ✅ **Картинки библиотеки — реальные люди-тёзки персонажа** (жалоба 15.08
  со скриншотами): "Гриффин" отдавал портрет жены Лоис вместо самого
  Питера (статья реально про него), "Алекс" (Alyx Vance, Half-Life) —
  случайное фото неизвестного мужчины начала XX века. Причина отличается
  от находок 12.08 (там были франшизные страницы) — здесь фильтры
  пропускали статьи о РЕАЛЬНЫХ людях с тем же именем/фамилией (футболист
  Alex Leake) и статьи о ДРУГИХ персонажах с общей фамилией (жена вместо
  мужа). Два фикса в `library.service.ts`: (1) проверка релевантности
  заголовка при 2+ значимых словах в имени требует совпадения ВСЕХ слов,
  не любого одного; (2) если у статьи есть описание, но в нём нет ни
  франшизного слова, ни маркера персонажа (список расширен) — результат
  теперь отбрасывается вместо молчаливого принятия. Плюс правило в
  промпте — при неполном имени в запросе (только фамилия) возвращать
  полное каноничное имя. Проверено симуляцией на 10 реальных случаях
  (Wikipedia API напрямую) — регрессий не найдено. Коммит `5103a2a`,
  задеплоено на Render.
- Подтверждено на проде: "Алекс" — картинка стала `null` вместо чужого
  фото (фоновый скрипт сам почистил, картинки не требуют Groq). **Не
  подтверждено на проде**: "Гриффин" — регенерация самой статьи (нужна для
  смены имени на "Питер Гриффин") упёрлась в дневную квоту Groq (502,
  2 попытки подряд) — отложено до следующей сессии, заметка бота оставлена
  `IN_PROGRESS`, не закрыта преждевременно.

**12.08.2026 (3)** — финальная проверка сессии + доисправление картинок библиотеки:
- По прямой просьбе пользователя ("проверь всё, чтоб не было казусов") прогнан
  полный контрольный проход по всему, что сделано сегодня, живьём на проде
  (Playwright): geolocation-skip, `BrandHeader` на всех 5 вкладках, дизамбигуация
  «Атрей» — всё подтверждено рабочим, 0 ошибок в консоли.
- ⚠️ **Найдено 2 преждевременно закрытых DONE-заметки бота** при аудите (тот
  самый паттерн, за который уже критиковал Марк) — возвращены в `IN_PROGRESS`:
  опрос от 09.08 (часть пунктов реально сделана, часть нет — не всё целиком)
  и заметка про баг с картинками библиотеки (см. ниже — фикс из сессии (2)
  чинил только НОВУЮ генерацию, старый закешированный «Гарри Поттер» и другие
  примеры из жалобы Марка при проверке на проде всё ещё отдавали старые
  плохие картинки).
- ✅ **Картинки библиотеки — доисправлено по-настоящему** (после того как
  сессия (2) закрыла тот же баг преждевременно):
  - Новый самоприменяющийся фоновый скрипт `refreshLibraryArticleImages
    2026_08_12.ts` — прогоняет все уже закешированные статьи через
    исправленную логику подбора картинки. `findCharacterImageUrl` не
    обращается к Groq (только Wikipedia REST API) — квота не тратится,
    можно чинить старые статьи скопом. Не блокирует старт сервера (сетевой
    цикл, не быстрая правка БД).
  - Найдено при живой проверке после первого деплоя: фильтр по описанию
    страницы (`looksLikeFranchisePage`) пропускал случаи вида "седьмой
    фильм о Гарри Поттере"/"американский анимационный ситком" — проверка
    была `startsWith` на всё описание целиком, а перед словом-маркером
    часто стоит прилагательное/числительное. Теперь проверяются первые
    3 слова + слова-маркеры "это статья о персонаже" (персонаж/герой/
    злодей/существо) имеют приоритет над франшизным фильтром.
  - Более глубокая причина для "Гарри Поттер": `characterName` (рус.) +
    `universe` (англ., от Groq) в одном запросе уводили поиск целиком в
    статьи о франшизе — сама статья о персонаже не попадала даже в топ-5
    выдачи. Добавлен запасной запрос чистым именем без universe.
  - ⚠️ **Этот же запасной запрос вскрыл старый баг и вызвал регресс**,
    пойманный ДО того, как задеплоенная версия провисела дольше пары
    минут: `titleLooksRelevant` сравнивал заголовок статьи через
    `.includes()` (подстрока), из-за чего "виктор" совпадал с началом
    "виктория" — "Виктор Сталь" стал находить портрет королевы Виктории
    Великобритании (реальный человек, не персонаж). Исправлено на
    сравнение ЦЕЛЫХ слов (очистка от скобок/пунктуации + точное
    совпадение токенов), задеплоено и повторно проверено на всех
    именах из жалобы Марка — регресс исчез, остальные картинки не
    пострадали.
  - Итог по всем именам из жалобы Марка (проверено напрямую через прод-API
    после финального деплоя): Гарри Поттер/Кратос/Геральт/Годзилла —
    верный портрет персонажа; Гриффин — портрет конкретного члена семьи
    (Лоис) вместо лого сериала (сама неоднозначность имени "Гриффин" —
    не баг подбора картинки, а неточность имени от Groq, не трогали);
    Годжо Сатору/Вольверин/Виктор Сталь — честно `null` (подходящей
    статьи в русской Википедии не нашлось, лучше отсутствие картинки,
    чем случайная чужая). Заметка `cmsm4eujk` закрыта DONE повторно —
    на этот раз с реальным подтверждением на актуальных данных.
- Три бэкенд-коммита (`69246df`, `6399979`, `3c96735`), автодеплой Render
  проверен вручную после каждого перед тем, как двигаться дальше (не
  полагался на одну лишь проверку статус-кода — сверял конкретные значения
  картинок в ответе API, как и раньше в этой сессии при проверке анкеты).

**12.08.2026 (2)** — библиотека знаний: 3 бага по фидбэку Марка от 09.08:
- ✅ **Русские имена персонажей** — промпт иногда возвращал транслитерацию
  вместо устоявшегося перевода ("Вольверин" вместо "Росомаха"). Добавлено
  явное правило с примерами в `SYSTEM_PROMPT`.
- ✅ **Картинки "из совсем других источников"** — `findCharacterImageUrl`
  иногда попадал не на статью персонажа, а на статью о медиафраншизе
  целиком ("Гарри Поттер (серия фильмов)" вместо персонажа, "Вселенная DC"
  вместо конкретного героя) — оттуда бралось лого/постер не того размера.
  Два новых фильтра: по короткому описанию статьи в Wikipedia REST API
  (у персонажа начинается со слова-роли — "герой"/"персонаж"/"злодей", у
  франшизы — со слова про сериал целиком) и по релевантности заголовка
  (для персонажей без хорошей статьи вообще, напр. "Виктор Сталь" — топ-3
  результата поиска были про совсем других героев — теперь честно `null`
  вместо чужой картинки). Заодно убран дубль слова в запросе, когда
  universe совпадал с именем персонажа.
- ✅ **Персонажи-тёзки** — на примере "Атрей" (сын Кратоса в God of War,
  Дом Атрейдесов в "Дюне", уже закешированный персонаж) — курируемый
  список `AMBIGUOUS_NAMES` в `library.service.ts`, при совпадении бэкенд
  отдаёт выбор вселенной вместо статьи, фронтенд показывает список,
  выбор запускает повторный поиск с уточнением. Список пока содержит
  только "Атрей" — расширять по мере новых жалоб (нет дешёвого способа
  определять неоднозначность имени автоматически без похода к ИИ на
  каждый запрос).
- Проверено: первые два — прямыми запросами к Wikipedia API локально (без
  деплоя) по всем проблемным примерам из фидбэка + контрольная группа уже
  работавших персонажей, регрессий нет. Третье — вживую (Playwright) на
  проде: выбор показывается и переискивает корректно; сама генерация
  упёрлась в дневную квоту Groq (502, известное повторяющееся
  ограничение) — ошибка отображается штатно, не крашит.

**12.08.2026** — срочные баги (геолокация блокировала вход, бренд-шапка) + находка про блокировку домена:
- ✅ **Геолокация больше не блокирует вход намертво** — Марк словил лично: перешёл
  по ссылке из Авито, геолокация не сработала (частая история во встроенных
  браузерах), а модалка `LocationRequiredModal` не закрывалась без разрешения
  вообще (`onRequestClose={() => {}}`, ни одной кнопки выхода) — тупик без
  возможности попасть в приложение. Добавлена кнопка «Пропустить — включу
  позже в профиле» (там уже есть рабочая кнопка для этого), флаг пропуска
  в AsyncStorage/localStorage per-аккаунт, повторно не показывается.
- ✅ **Брендовая плашка «Markeltoys» на всех вкладках** — до 24.07 тут был
  нативный header с названием экрана, убрали как дублирующий подпись
  таб-бара. Марк попросил вернуть, но под бренд, не под название раздела.
  Новый общий компонент `BrandHeader`, добавлен на все 5 вкладок + админку
  (7 файлов, включая отдельный `MapScreen.web.js`, который перекрывает
  обычный `MapScreen.js` на вебе — про него легко забыть).
- ⚠️ **Найдена причина «без VPN не заходит»** — это НЕ баг в коде: домен
  `*.workers.dev` целиком заблокирован Роскомнадзором в России (проверено
  через поиск). Значит любая ссылка на приложение (Cloudflare Workers,
  `holy-grass-59e8.ksele52.workers.dev`) физически не открывается у
  российских пользователей без VPN — это прямой блокер цели «нарастить
  500 пользователей». Решение — переезд на свой домен (например
  markeltoys.ru/.com), подключённый к тому же Cloudflare Worker; технически
  просто, но нужно купить домен и это решение/бюджет Марка — по просьбе
  пользователя отправлено ему через тг-бота с объяснением, ждём ответа,
  сам домен не покупал и не менял.
- Все фиксы проверены вживую (Playwright): геолокация — пропуск работает
  и переживает перезагрузку; бренд-шапка — все 5 вкладок + карта (обе
  версии), 0 ошибок в консоли. Задеплоено (Cloudflare `npm run deploy`).

**10.08.2026 (2)** — обязательная анкета коллекционера при регистрации:
- ✅ По прямой просьбе Марка (09.08) — регистрация теперь в 4 шага вместо 3:
  данные → email → фото (аватар + **минимум 3** фото коллекции, было
  опционально) → анкета (возраст, что коллекционирует — мультивыбор
  фигурки/статуи/приставки/игры/карточки, стаж в годах с опцией «меньше
  года», любимая франшиза/персонаж, о себе 100–3000 символов).
- Бэкенд: новые поля `User.age/collectorTypes/collectingSinceYears/
  favoriteFranchise/favoriteCharacter`, `bio` расширен 500→3000 символов.
  Поля nullable — обязательность живёт только во фронтенд-флоу
  регистрации (как уже было с фото профиля), не форсируется задним числом
  для существующих пользователей. `age` не публичный (только свой профиль),
  остальное — в публичном профиле и на карте (искать единомышленников).
- Простая эвристика «не ерунда» без ИИ (`isLikelyGarbage` — текст должен
  содержать буквы, не быть повтором одного символа >60%) — сознательно
  не через Groq, квоты и так впритык на библиотеку знаний.
- Проверено вживую (Playwright): валидация ловит и пустые поля, и мусор
  ("ааааааааа" → ошибка); поля реально доезжают до БД и возвращаются
  обратно (проверено прямым API-запросом на проде после деплоя). Полный
  email-verification флоу (шаги 1→2) сквозным браузерным тестом не
  прогнать без доступа к реальному инбоксу — шаги 3–4 проверены отдельно
  через временную подмену начального `step`, откачена перед коммитом.
- Коммиты: `e14a5cc` (backend, Render автодеплой), `a8d6d7c` (frontend,
  `npm run deploy` на Cloudflare).

**10.08.2026** — размытая подложка вместо плоских полос под артом карт:
- ✅ Побочный эффект вчерашнего фикса (cover→contain, карта не режется) —
  плоские тёмные полосы по краям рамки, выглядело пусто. По решению
  пользователя заменено на приём Spotify/Apple Music: тот же арт вторым
  слоем растянут на весь фон (cover) с блюром и затемнением, поверх —
  чёткая (contain) картинка. `CardImage` в `cardArt.js` — правка одна,
  применилась сразу во всех местах отображения карт (рука, стол, лупа,
  коллекция).
- Технический нюанс: подложка отрисована с отступом (-14px) за границы
  рамки и обрезается через `overflow:hidden` на обёртке — иначе у самого
  блюра на границе видна более светлая кромка (недосэмплированный край).
- Проверено вживую на проде через Playwright (снова доступен после
  отключения) — коллекция и активный бой (рука + стол), 0 ошибок в
  консоли, визуально подтверждено на скриншотах.

**09.08.2026** — переименование в Markeltoys + фиксы по фидбэку Марка:
- ✅ Марк ответил на «план развития» (документ-опрос от Кирилла): выбрал
  **пассивный доход**, но с оговоркой — рано говорить о монетизации/юрлице,
  пока нет аудитории (цель — первые 500 пользователей). Хочет: полноценное
  приложение в сторе (не веб-ссылку), автоматизацию продвижения. Отдельный
  вопрос — бот, который сам комментирует в гик-пабликах с редиректом к нам
  (этический/репутационный риск, не начато, нужно обсуждение).
- ✅ **Видимый бренд переименован** Collector's Realm → **Markeltoys** (по
  просьбе Марка — так его знают подписчики): логотип/футер логина, канал в
  новостях, заголовок вкладки браузера, `app.json` name (имя под иконкой
  при установке), `bundleIdentifier`/`package` `com.collectorsrealm.app` →
  `com.markeltoys.app` (безопасно — в сторы ещё не публиковались), тема и
  отправитель писем (код регистрации/сброс пароля). Инфраструктурные
  идентификаторы НЕ тронуты (сломали бы интеграции): хост Render, Cloudflare
  Worker `holy-grass-59e8` (текущий URL), Cloudinary preset, домен писем.
- ✅ **Карты в игре больше не обрезаются** — подтверждено живьём (Playwright
  вернулся после отключения): `resizeMode`/`backgroundSize` cover → contain
  в общем `CardImage` (весь арт карт квадратный 1:1, а рамки карт нигде не
  квадратные). **Побочный эффект** — карта теперь смотрится мельче, с
  тёмными полосами по краям; технически не обрезается, но не факт что
  красиво — решение о финальном виде за пользователем.
- ✅ **Логин-баг найден и исправлен** — жалоба Марка "разлогинивает каждые
  2-3 дня" воспроизведена вживую (клик по вкладке «Игра» дал 401 на
  залогиненной сессии). Токен жил 7 дней — поднят до 90 (полноценного
  refresh-токена нет, это компромисс, не идеал с т.з. безопасности).
- ✅ Реальная причина обрезки картинки библиотеки (найдена в этой же
  сессии чуть раньше) — `{height: undefined}` в инлайн-стиле НЕ
  переопределяет `height` из класса `StyleSheet.create` на react-native-web
  (RNW не эмиттит `undefined` в inline style, класс остаётся в силе).
  Убрал `height` из класса совсем — теперь всегда явное значение.
- Закрыты 4 старые заметки бота (`cmr4286il`, `cmrl0r7mz`, `cmrv2ih4q`,
  `cmrv2kj9i`) по решению пользователя — считает решёнными.
- **Новые открытые пункты от Марка** (не начаты, ждут приоритезации):
  обязательная анкета при регистрации (3 фото коллекции + форма: ник,
  возраст, что коллекционирует, стаж, любимая франшиза/персонаж, о себе),
  реальное приложение в сторе вместо веб-ссылки, боссы с уникальными
  пассивками в игре + новая колода после финального босса.

**05.08.2026 (3)** — автодополнение поиска (библиотека + магазин):
- ✅ Пункты 2-3 из заметки Марка 05.08: выпадающий список подсказок по
  первым буквам. Библиотека — новый `GET /api/library/suggest` (prefix-
  поиск по уже закешированным `characterName`, регистронезависимый, без
  обращения к ИИ), фронтенд дебаунсит запрос на 250мс. Магазин — то же
  самое чисто на клиенте (товары и так уже загружены в память для сетки
  карточек, простой prefix-фильтр по названию, бэкенд не нужен).
- Дропдаун в обоих местах — обычный поток вёрстки (не absolute/z-index),
  просто отодвигает контент под собой; закрывается при выборе подсказки
  или при выполнении поиска.
- Проверено на живом проде через Playwright: в библиотеке "Лю" → "Лю Кан"/
  "Лю Кен"/"Люк Скайуокер", в магазине "De" → "Deadpool"; выбор подсказки
  корректно подставляет полное имя. Коммиты `bd7f84f` (backend, Render
  автодеплой) и `f8e6ccf` (frontend).
- Оба пункта из заметки Марка 05.08 (нечёткий поиск + автодополнение)
  теперь закрыты полностью.

**05.08.2026 (2)** — нечёткий поиск в библиотеке (толерантность к опечаткам):
- ✅ По просьбе Марка (05.08, пункт 1): "Аладин"/"Алодин"/"Оладин" теперь
  находят статью "Аладдин" без похода к ИИ. `findClosestSlug` в
  `library.service.ts` — Левенштейн с порогом по длине имени (короткие
  имена ≤4 симв. разрешают 1 опечатку, длиннее — 2-3), сравнивается со
  слагами уже закешированных статей ПЕРЕД обращением к Groq. Экономит
  скудную дневную квоту (см. [[project_groq_quota_risk]]) — опечатка не
  тратит ни одного вызова ИИ, если персонаж уже есть в библиотеке.
- ✅ Попутный фикс: новые статьи теперь кешируются под слагом КАНОНИЧНОГО
  имени, которое вернул ИИ, а не под опечаткой из исходного запроса —
  иначе первая опечатка нового (ещё не закешированного) персонажа плодила
  бы отдельную статью под своим слагом, и только повторный поиск с той же
  самой опечаткой находил бы кеш.
- Проверено на проде прямыми запросами к API (без браузера) — все 3
  варианта опечатки из примера Марка ("Аладин", "Алодин", "Оладин")
  корректно находят "Аладдин" за ~1.6с (без обращения к Groq). Коммит
  `b725ab9` (backend), Render автодеплой.
- **Не сделано из той же пачки 05.08**: автодополнение выпадающим списком
  по первым буквам — и в библиотеке, и в общем поиске магазина (пункты
  2-3 той же заметки) — отдельная фича, не начата.

**05.08.2026** — качество статей библиотеки + 3 бага по фидбэку Марка (03.08):
- ✅ Промпт статей библиотеки (`library.service.ts`) доработан по конкретным
  замечаниям Марка на примере статьи про Аладдина: запрет повторять имя
  персонажа в начале подряд идущих абзацев (читалось "дёшево"), запрет
  калек с английского вроде "иконический" → "культовый"/"легендарный",
  требование мысленной вычитки грамматики/пунктуации перед выдачей,
  правило "не выдумывать факты" распространено на всю статью (было только
  для раздела про мерч). Коммит `063f144` (backend), Render автодеплой.
  **Не сделано**: автоматическая перегенерация уже опубликованных старых
  статей под новый промпт — Groq-квота слишком мала (~25-30 статей/день
  на весь трафик), массовый форс-рефреш сжёг бы её впустую; остаётся
  staff-only `?refresh=1` точечно по одной статье.
- ✅ **Картинка персонажа в статье обрезалась сверху/снизу** (жалоба
  с скриншотом статьи про Аладдина) — `articleImage` была втиснута в
  фиксированные 220px с `resizeMode="cover"`, портретные фото (обычная
  ориентация фото персонажей в Wikipedia) резались. `LibraryScreen.js`
  теперь считает высоту по РЕАЛЬНОМУ соотношению сторон картинки
  (`Image.getSize`), с разумными границами 0.62–1.9 на случай совсем
  панорамных/вытянутых кадров.
- ✅ **Выделение текста при увеличении карты в бою** — `noCalloutStyle`
  (гасит `userSelect`) был повешен только на арт карты внутри
  `CardZoomModal`, а не на всю карточку (имя/редкость/статы/текст
  эффекта) — именно эти Text-элементы и выделялись при долгом нажатии.
  `userSelect: none` наследуется CSS-каскадом, перенесли на общую
  обёртку модалки — один раз гасит выделение везде внутри.
- ✅ **Рука карт "обрезалась"** — оказалось не багом арены (стол/боевое
  поле проверено на 3 размерах экрана через Playwright, не обрезается),
  а горизонтальным `FlatList` руки без индикатора скролла: когда карт
  больше, чем влезает по ширине, лишние тихо уходят за правый край без
  единой подсказки — выглядит как "карты пропали". Добавлено лёгкое
  затухание с правого края (`LinearGradient`, `BattleScreen.js`),
  появляется только когда контент реально не помещается целиком
  (сравнение `onContentSizeChange`/`onLayout`).
- ✅ Тг-бот Марка (см. [[reference_mark_telegram_bot]]) — правило: в конце
  любого отчёта об обновлениях для него теперь обязательно добавлять
  ссылку на веб-версию приложения (прямая просьба 03.08).
- Проверено в браузере (Playwright) на 390×844, 844×390, 360×640 —
  0 ошибок в консоли, затухание руки подтверждено визуально на 360×640.
  Коммит `9341296` (frontend) запушен в GitHub — **на Cloudflare ещё не
  задеплоено**, нужен `npm run deploy` (см. [[reference_frontend_deploy]]).
- **Осталось из пачки задач Марка** (05.08, не начато): нечёткий поиск
  с опечатками и автодополнение выпадающим списком — и в библиотеке
  знаний, и в общем поиске магазина; масштабирование библиотеки до тысяч
  статей (упирается в Groq-квоту, см. [[project_groq_quota_risk]]).

**01.08.2026** — баг лупы карты + фиксированный экран боя + доработка библиотеки знаний:
- ✅ **Лупа карты стабильно открывается** (было: мигала и закрывалась сразу же,
  "выскакивают какие-то функции" в терминах Марка) — на вебе долгое нажатие
  открывало `CardZoomModal`, но отпускание пальца/мыши приходилось уже на новый
  верхний элемент (сам backdrop модалки) и читалось им как тап по фону,
  закрывая её в тот же кадр. Фикс — игнорировать закрытие в первые ~350мс
  после открытия (`CardZoomModal.js`). Заодно арт карты переведён с `<Image>`
  на CSS `background-image` (`CardImage` в `cardArt.js`) — на Android Chrome
  долгое нажатие на `<img>` открывало системное меню "сохранить картинку"
  поверх лупы, у div такого меню нет в принципе. Лупа добавлена и в коллекцию
  карт (`GameScreen.js`), где её раньше не было вовсе.
- ✅ **Экран боя снова фиксированный** (без общего скролла, введённого 21.07) —
  по прямой новой просьбе Марка: "убери текстовый вариант боя, ничего не нужно
  прокручивать, стол и рука должны быть видны всегда". Лог хода убран из
  основного вида, открывается отдельным окном по кнопке "📜 Лог".
- ✅ **Найдена и починена регрессия от этого же фикса**: на компактной высоте
  (телефон-ландшафт, ~390px) банnер+рука+статус-бар съедали почти всё место —
  ряд существ игрока обрезался `overflow:hidden` арены и был нетапаемым (Марк:
  "нет возможности выбрать карту на столе для удара"). Размер слота теперь
  считается от РЕАЛЬНО измеренной высоты арены (`onLayout`), а не от догадки
  по высоте окна; на компакте ряд колоды/сброса убран из арены (счётчики
  перенесены в статус-бар), баннер и кнопка хода ужаты.
- ✅ **Подзаголовки в статьях библиотеки** — по образцу от Марка (структура:
  История создания / Биография / Способности / Влияние на индустрию / Вывод)
  промпт переписан под строгую 5-секционную структуру с маркером `## Заголовок`
  (не настоящий markdown, простой парсимый разделитель), фронтенд
  (`LibraryScreen.js`) парсит и рендерит каждый раздел жирным подзаголовком.
- ✅ **Картинка персонажа в шапке статьи** — реальный кадр/постер из
  **русской** Википедии (не английской — имена от Groq приходят на русском,
  поиск по en.wikipedia либо ничего не находил, либо путал с случайной похожей
  статьёй), с `universe` вторым словом запроса для дизамбигуации (иначе
  "Кратос" находил греческого бога вместо героя God of War). Новое поле
  `LibraryArticle.imageUrl` (nullable, best-effort — не блокирует статью, если
  картинка не нашлась). Сознательно НЕ AI-генерация — в отличие от карт игры
  (вымышленные монстры), здесь чужие трейдмарк-персонажи и лица актёров,
  решение пользователя не подделывать это нейросетью.
- ✅ Собран курируемый список ~240 известных персонажей гик-культуры
  (`docs/library-characters-list.txt` в бэкенд-репо, без обращения к ИИ) —
  стартовая точка для будущей пакетной генерации, отправлен Марку.
- ⚠️ **Дневной лимит токенов Groq (100k) — активный, регулярно повторяющийся
  блокер**, общий с расшифровкой голосовых в тг-боте Марка и живым
  пользовательским трафиком библиотеки. При тестировании сегодня один запрос
  ("Гамора") вернул статью, обрезанную посреди предложения, и всё равно
  сохранился в кеш как "готовый" (`hasForeignScript()` не ловит такой брак).
  Посчитан реалистичный бюджет (~15-20 статей/день сверх органического
  трафика) и отправлен Марку. Решение отложено пользователем — если нужна
  стабильная скорость, лучший вариант: отдельный Groq-ключ только под
  библиотеку (см. память Claude `project_groq_quota_risk`).
- Все правки на фронтенде и бэкенде задеплоены (Render автодеплой + `npm run
  deploy` на Cloudflare), проверено в браузере (Playwright) на 3 размерах
  экрана (десктоп 1280×800, телефон-ландшафт 844×390, телефон-портрет
  412×915) + реальным сценарием розыгрыша карты/атаки.

**26.07.2026** — тг-бот задач Марка научен принимать фото + докручен откат карт:
- ✅ Бот Марка (`markBot.routes.ts`) обрабатывал только `message.text`/`message.voice` —
  присланные фото молча игнорировались (сообщения без текста не сохранялись вообще).
  Обнаружено при разборе заметок 21.07 («я скидывал картинки, как должны выглядеть
  карты») — картинок нигде не было, восстановить нечего. Добавлена загрузка
  `message.photo` через Telegram file API → Cloudinary (`uploadTelegramPhoto` в
  `telegram.service.ts`) → новое поле `MarkNote.imageUrl` (тип `PHOTO` в
  `MarkNoteType`). Подпись к фото — как текст заметки. Коммит `d99d886`
  (`collectors-realm-backend`), Render автодеплой.
- ✅ По прямому запросу — докручен откат реюскина карт от 20.07
  (`revertCardRoster2026_07_20.ts`): старая версия матчила карты только по
  имени реюскина одним проходом при старте — если апдейт для какой-то карты
  не применился (напр. рестарт сервера в процессе записи), она осталась бы
  зависшей с новым артом навсегда, т.к. при следующем старте её имя уже не
  совпало бы ни с одним `matchName`. Переписан на самовосстанавливающуюся
  логику: матчинг по (имя+статы) — статы реюскин не менял — чинит и
  «застрявшие» карты (ещё под новым именем), и карты с уже верным именем, но
  недокатившимся `imageUrl`/`boardImageUrl`. Идемпотентно, гоняется на каждом
  старте сервера (уже был в цепочке `index.ts`). Коммит `7dd852b`.
- ✅ **Найдена и исправлена отдельная старая порча ростера карт**, не
  связанная с реюскином — вскрылась при аудите всех 20 карт через
  `/api/cards/admin/all` (после того как пользователь дал реальный
  админ-аккаунт). 3 карты отсутствовали целиком (Дрон, Воин, Следопыт), а
  вместо них было по 2 экземпляра других карт (Лицехват, Юнблад, Разведчик).
  Корень — баг ещё в `rename-cards-canonical-2026-06-18.sql` (строка 49):
  карта-филлер «Хищник-разведчик» (добавлена 12.06) должна была стать 10-м
  хищником «Волк» (единственный GOLD-тир, которого не хватало среди
  исходных 9), но скрипт по ошибке переименовал её в уже существующее имя
  «Разведчик» — отсюда дубликат; аналогичная порча задела и сторону Чужих.
  Новый стартап-скрипт `fixCardRosterGaps2026_07_26.ts` чинит 3 конкретные
  строки по id (данные — из `seed-cards.ts`/`set-card-images-2026-06-25.sql`
  для Дрон/Воин, из уже восстановленного `revertCardRoster2026_07_20.ts` для
  Следопыт). Коммит `c062e04`, задеплоено. **Проверено через реальный
  админ-аккаунт** (`ksele52@gmail.com`, полный список 20 карт).

**24.07.2026** — убрана дублирующая шапка экранов + библиотека знаний гик-культуры (MVP):
- ✅ По заметке Марка в тг-боте (`cmrys47cq`) убран нативный header у
  корневых экранов вкладок (Магазин/Карта/Игра/Профиль/Админ) — раньше он
  дублировал название, уже видное в нижнем таб-баре. Вложенные экраны
  (Товар/Чат/Профиль пользователя и т.д.) header не потеряли — там он
  нужен для заголовка и кнопки «назад». Контент сдвинут на
  `insets.top` вместо header'а во всех затронутых экранах.
- ✅ Новая фича «Библиотека знаний» (заметки `cmryrvsqt` IN_PROGRESS,
  `cmrys737w` DONE) — энциклопедия персонажей гик-культуры: `LibraryScreen.js`
  (поиск + статья), точка входа — иконка книжки (открытая, не в кружке,
  чёрный контур, подпись «Библиотека», по спеке Марка) на главном экране
  магазина. Бэкенд: `GET /api/library/article?q=...` — статья генерируется
  через **Groq** (`llama-3.3-70b-versatile`, тот же ключ, что и для
  расшифровки голоса) и кешируется в новой модели `LibraryArticle` по
  `slug` — повторный поиск того же персонажа не бьёт по ИИ снова. Лента
  «Недавно искали» (`LibrarySearchLog`, `GET /api/library/recent`) —
  10 последних уникальных запросов, тап по чипу повторяет поиск. Базовый
  фильтр мата/18+ (`isBlockedQuery` в `library.service.ts`) блокирует
  запрещённые запросы ещё до обращения к ИИ.
- **Найденный и исправленный баг**: Groq изредка портит русский текст —
  подмешивает иероглифы CJK, replacement-символы (`�`, битые байты) или
  слипшиеся русско-латинские слова без пробела («вpopularной»). Ужесточили
  промпт («строго на русском»), снизили `temperature` 0.7→0.4, добавили
  одну попытку перегенерации при обнаружении брака перед сохранением в
  кеш (`hasForeignScript()`), плюс самоприменяющаяся чистка уже
  закешированных бракованных статей при старте сервера
  (`cleanupBadLibraryArticles.ts`, тот же принцип, что и `backfillCardFactions` —
  без ручных SQL-шагов в Supabase).
- Пока НЕ сделано (по решению — MVP сначала): подгрузка готового файла
  персонажей от Марка (запрошен через бота, ждём) для настройки формата
  статей под его образец.
- Проверено вживую в браузере (Playwright): поиск → статья, повторный
  поиск из кеша, блокировка «порно», лента последних запросов и клик по
  чипу — всё работает, 0 ошибок в консоли. Задеплоено оба конца (backend
  push+Render, frontend `npm run deploy`).

**21.07.2026** — экран боя: один вертикальный скролл вместо расчёта высоты:
- По прямой просьбе пользователя ("посмотри как на похожих сайтах, чтобы
  всё было видно, можно было двигать экран вверх/вниз, кнопки ничего не
  закрывали") переписан `BattleScreen.js`: раньше высота арены боя
  высчитывалась в пикселях под оставшееся место (`arenaHeight = height -
  insets - playerBarH - bottomH - LOG_HEIGHT`), а низ экрана (лог, рука,
  кнопка «Закончить ход») вообще не скроллился — при ошибке расчёта на
  нестандартном экране низ пропадал без возможности докрутить.
- Теперь весь экран — один `ScrollView` (арена → полоса игрока → лог →
  рука → кнопка хода), как в большинстве веб-карточных игр: что бы ни
  случилось с размерами, всегда можно долистать до любого элемента.
  Заодно убран весь `compact`-режим (уменьшенные карты/бейджи под "тесный"
  экран) — он был нужен только чтобы всё поместилось без скролла, раз
  скролл есть всегда — везде комфортный полный размер. Лог хода (был
  отдельный вложенный `FlatList` с фиксированной высотой) стал обычным
  списком `View` внутри общего скролла — записей за бой немного,
  виртуализация не нужна.
- Проверено в браузере на 3 размерах (телефон-ландшафт 844×390, портрет
  390×844, десктоп 1280×800) + полный цикл хода (розыгрыш карты, атака,
  активация невидимости, конец хода ×3, ресайз окна прямо во время боя) —
  везде видно всё целиком, ничего не обрезается/не перекрывается.
- Заодно прогнал общий краш-тест по сайту (магазин: поиск/фильтры/карточка
  товара, вишлист, карта, профиль, логаут/логин) — 0 ошибок в консоли
  везде. Деплой: `8a516c8`, `npm run deploy` на Cloudflare.
- Также разобрал бэклог задач Марка из бота: закрыл заметку про поворот
  экрана (в проде), обнаружил и закрыл заметку про доступ к магазину — она
  была реализована ещё 10.07, но не отмечена; закрыл заметку "5 идей
  заработка карт" (текстовые идеи уже даны раньше, сама реализация — это
  и есть `cmqnncy4c`, которую Марк попросил отложить). Осталось открытым:
  `cmr4286il` (ждём скриншот от Марка), `cmrl0r7mz` (визуал стола — не
  трогать без явного нового запроса, тема уже раз не прижилась 20.07),
  `cmqnncy4c` (заработок+слияние карт, отложено самим Марком).

**20.07.2026 (4)** — ОТКАТ реюскина и визуала карт (сессии (3) и (2)):
- Пользователь увидел результат и сказал прямо: "выглядит отвратительно,
  откати всё до старых рамок". Полный откат:
  - Бэкенд: `revertCardRoster2026_07_20.ts` (заменил `reseedCardRoster...`) —
    восстанавливает оригинальные имя/статы/эффект/арт по 18 картам, обратно
    матчит по НОВОМУ имени, `boardImageUrl` очищается в null. Оригинальные
    URL взяты из исторических миграций (`set-card-images-2026-06-25/07-01`).
  - Фронтенд: `cardArt.js`/`HandCard.js`/`BoardSlot.js`/`CardZoomModal.js`/
    `GameScreen.js`/`AdminScreen.js` — `git checkout` до коммита `ab644f3`
    (состояние ДО этой сессии). Простая цветная рамка + `RarityCorners`
    (золотой вензель только у GOLD) вернулись, группировка коллекции по
    фракции — тоже откатилась к старым чипам "Как получены"/"По редкости".
  - `BattleScreen.js` НЕ трогали — свободный поворот экрана убран по
    отдельной явной просьбе в той же сессии, не связан с "рамками".
- Проверено в браузере и через прод-API — все 20 карт вернулись к
  оригинальным именам/статам/арту, `boardImageUrl: null` у всех.
- **Итог по теме "визуал карт по референсу Марка"**: НЕ прижился, откачен
  полностью. Если пользователь снова попросит визуал ближе к референсу —
  начинать с чистого листа, не пытаться реанимировать код из (2)/(3), там
  результат уже был признан неудачным.

**20.07.2026 (3)** — полный реюскин 18 из 20 карт под референс Марка (ОТКАЧЕНО, см. (4) выше):
- Пользователь дал 2 скрина референса (полные карты с резной рамкой/именем/
  маной/статами/текстом, срисованные с ESO:Legends-стиля) и попросил
  использовать их напрямую как арт карт. Имена на референсе почти не
  совпадали с нашими 20 (только «Следопыт»/«Лицехват»), и на референсе всего
  8 уникальных имён для Чужих (не 10) — решение пользователя: реюскинить
  18 карт (новое имя+арт, СТАРЫЕ характеристики/эффекты доноров), «Дрон» и
  «Кладка яиц» не трогать. ~15 новых уникальных эффектов с картинок (Прорыв,
  добор карты, урон герою при убийстве и т.д.) сознательно НЕ реализованы в
  движке — по решению пользователя это только новые имена над старыми
  механиками, текст на самой картинке (баковый) поэтому местами не совпадает
  с тем, что карта реально делает в бою — известный и принятый трейд-офф.
- ✅ Полная сборка: `sharp` (уже был в зависимостях) нарезал оба листа 5×2 на
  20 ячеек (целая карта, для руки/коллекции) + 20 портретов (обрезка ~19-36%
  от левого края/20% от верха ячейки — под резную рамку, для стола боя).
  Все 40 картинок залиты в Cloudinary (`cards/<translit-имя>[-board].png`).
- ✅ `Card.boardImageUrl` (новое поле) — портрет-кроп для стола, отдельно от
  `imageUrl` (целая карта). `src/startup/reseedCardRoster2026_07_20.ts`
  (бэкенд, самоприменяющийся при старте, без ручного SQL) — переименовывает
  18 карт. **Важный найденный баг**: матчинг по `card.order` (казалось бы
  стабильный индекс) тихо переименовал бы случайные карты — order на проде
  давно разошёлся с индексами `seed-cards.ts` (легаси вставок/переименований
  12.06/18.06). Пойман до того, как проработал на проде дольше пары минут,
  заменён на снимок `findMany` в память + сравнение имён через `.trim()` в
  JS (одна карта, «Разведчик», не матчилась точным SQL-равенством — похоже,
  невидимый символ от старой ручной правки).
- ✅ Фронтенд: `HandCard.js`/`CardZoomModal.js`/`GameScreen.js` — если у карты
  есть `boardImageUrl`, показывают `imageUrl` как есть (никаких своих
  бейджей/рамки/имени — задвоило бы то, что уже нарисовано на картинке).
  `BoardSlot.js` — на столе ВСЕГДА `boardImageUrl || imageUrl` + свои живые
  бейджи HP/атаки (там урон/ауры меняются в реальном времени, дублировать
  статичные цифры с картинки нельзя). Старые карты без `boardImageUrl`
  (просто квадратный портрет) — везде как раньше, ничего не сломано.
- Проверено в браузере (Playwright) — коллекция и бой (рука + стол) выглядят
  как референс, 0 ошибок в консоли. Деплой: бэкенд (`4289c35`, после 2
  промежуточных фиксов бага с order/именем), фронтенд (`ccb1848`,
  `npm run deploy` на Cloudflare, второй раз — первая попытка упала по
  таймауту сети к Cloudflare API, retry через `npx wrangler deploy` помог).

**20.07.2026 (2)** — бэкафилл без ручного шага + группировка коллекции + свободный поворот в бою:
- ✅ Новое поле `Card.faction` (`ALIEN`/`PREDATOR`) — по референсу Марка (макет
  карты с раскладкой мана/имя/фракция/атака-защита и рамками, различающимися
  по фракции). SQL-бэкафилл `prisma/set-card-factions-2026-07-20.sql` —
  **пользователю нужно выполнить его в Supabase SQL Editor**, до этого все
  карты идут по старому фоллбэку (без гем-уголков/тинта).
- ✅ Найден и исправлен баг: ауры усиления (buff_allies/acid_blood_buff/
  stealth_buff — Преторианец/Волк и т.п.) считались один раз на весь стол
  владельца и применялись ко ВСЕМ его картам, хотя текст эффекта обещает
  усиление только своей фракции («усиливает всех Чужих»/«всех Хищников»).
  Пофикшено и на бэкенде (`auraAttackBonus` в `cards.routes.ts`, реальный
  урон в бою), и на фронте (`auraAttackBonus`/`effectiveAttackOf` в
  `BattleScreen.js` — теперь считается отдельно на каждое существо по его
  фракции, не одним числом на весь стол).
- ✅ `cardArt.js`: `FACTION`-палитра (тис `#2dd4bf`/бронза `#b06a2c` —
  намеренно не переиспользует зелёный/золотой, те уже заняты смыслом
  «жизни»/«редкость GOLD»), `CardCorners` (гем-уголки по фракции на любой
  редкости, не только GOLD, с фоллбэком на старые `RarityCorners` для карт
  без faction), `FactionLabel` (подпись «Чужой»/«Хищник» — только там, где
  есть место: увеличенная карта, обзор в админке), `nameplateGradient`
  (градиент подложки имени с лёгким тинтом фракции). Применено в
  `HandCard.js`, `BoardSlot.js`, `CardZoomModal.js`, `GameScreen.js`
  (коллекция), `AdminScreen.js` (вкладка «Игра»).
- ✅ Виньетка вместо сплошного затемнения фона (`BattleScreen.js` — арена
  боя, `GameScreen.js` — коллекция): темнее у краёв/шапки, светлее в
  середине, где сам контент — более атмосферно и лучше читаемость текста.
- Проверено в браузере (Playwright, dev-сервер) — коллекция и активный бой
  рендерятся без ошибок в консоли, старые (без faction) карты падают на
  прежний вид корректно. Задеплоено: бэкенд запушен (`17b84c4`, Render
  автодеплой), фронтенд закоммичен (`a7e0ee3`) и задеплоен на Cloudflare
  (`npm run deploy`).
- **Не сделано в этой сессии** (по решению — не тащить в один заход):
  полноценные орнаментированные рамки как на референсе Марка (кость/латунь
  с вензелями) — решили не гнаться за фигуративной SVG-резьбой на карте
  60px (нечитаема на такой площади, риск «дешёвого» вида), вместо этого
  выбран более устойчивый язык «гем-уголок + тинт + подпись», масштабируемый
  от 60px до полноразмерной карты. Новый арт фона арены/коллекции (не
  код, а сама картинка) — тоже не сделан, это контент-задача через тот же
  пайплайн Leonardo.ai, что и арт карт, а не то, что можно закодировать.
  Если нужна более близкая к референсу «резная» рамка — обсудить отдельно
  (векторные орнаменты только для крупных видов карты типа CardZoomModal,
  где есть площадь).

**20.07.2026 (2)** — бэкафилл без ручного шага + группировка коллекции + свободный поворот в бою:
- ✅ Оказалось, что faction на проде так и остался `null` — SQL-бэкафилл
  требовал ручного запуска в Supabase, пользователь это заметил как "ничего
  не работает". Вместо ручного SQL — `src/startup/backfillCardFactions.ts`
  (бэкенд) теперь заполняет faction САМ при каждом старте сервера
  (идемпотентно, фильтр `faction: null`) — больше никаких ручных шагов после
  деплоя для этого поля.
- ✅ Коллекция карт (`GameScreen.js`) перестроена: карты группируются по
  фракции (заголовки «Хищники»/«Чужие», по референсу Марка), внутри группы —
  по редкости (GOLD → COMMON), при равенстве — по `card.order`. Убран старый
  чип-переключатель "Как получены"/"По редкости" — теперь это единственная
  раскладка. `buildCollectionRows()` строит плоский список строк (заголовок
  или пара карт) вместо `numColumns` у FlatList, тайл карты вынесен в
  `CollectionCardTile`.
- ✅ Убран принудительный поворот экрана в бою (по прямой просьбе — старое
  требование крутить телефон мешало): `BattleScreen.js` больше не вызывает
  `ScreenOrientation.lockAsync(LANDSCAPE)` при входе (и не возвращает портрет
  при выходе) — ориентация полностью свободная, как и на всех остальных
  экранах (это был единственный экран с ручным лочем вообще). На вебе убран
  блокирующий экран-заглушка "Поверните телефон" — бой теперь рендерится в
  любой ориентации/размере окна.
- ✅ Найдена реальная причина жалобы "всё маленькое и неудобное при повороте":
  компактный размер карт/баннеров зависел от `width > height` (т.е. от
  соотношения сторон), а не от реальной высоты экрана — на десктопе/планшете
  (широкое окно, но высоты полно) игра рисовалась в том же крошечном
  "телефон-в-ландшафте" режиме, что и на реальном телефоне 390×844.
  Переменная переименована в `compact = height < 500` (реальный дефицит
  высоты) — раздутые "compact"-размеры (48px слот, 54×76 карта в руке)
  остались точно как были и тестировались на реальном телефоне, но теперь
  включаются только при реальной нехватке высоты. На широких окнах слоты
  стола выросли с 48 до 72-88px, карта в руке — с 54×76 до полных 96×136.
- Проверено в браузере (Playwright) на 3 размерах: десктоп 1280×800 (стало
  заметно крупнее, как и просили), телефон-ландшафт 844×390 (не изменилось —
  тот же протестированный компактный вид), телефон-портрет 390×844 (больше
  не блокируется экраном "поверните телефон", бой играбелен, хоть стол и
  тесноват по ширине — это уже честное ограничение самой ширины экрана, а не
  искусственный блок). 0 ошибок в консоли на всех трёх.
- Задеплоено: бэкенд (`0314411`, Render автодеплой), фронтенд (коммит после
  этой сессии, `npm run deploy` на Cloudflare).

**10.07.2026 (3)** — вкладка «Игра» в админке Марка:
- ✅ Новая вкладка `AdminScreen.js` (для `isStaff` — ADMIN и MODERATOR): обзор
  всех 21 карты темы с артом (`CardArt` из `cardArt.js`), редкостью и полным
  набором характеристик (мана/атака/жизни/текст эффекта) — Марк явно просил
  именно визуальный просмотр, не только текстовое описание. Плюс базовая
  статистика: бои по статусам (в процессе/победа/поражение), число игроков
  с картами, топ карт по суммарному владению.
- Бэкенд: `GET /api/cards/admin/all` и `GET /api/cards/admin/stats`
  (`cards.routes.ts`), доступ только ADMIN/MODERATOR (`isStaffRole` на
  `req.userRoles` из JWT).
- Попутно обнаружено и учтено: в `AdminScreen.js` уже были несохранённые
  правки пользователя (доступ MODERATOR к вкладке «Товары», сделано вне
  этой сессии) — разнесены по разным коммитам, чтобы не смешивать с
  вкладкой «Игра» (реконструировано через сравнение с HEAD-версией файла).
- Заметка Марка `cmrd2w8i9` (09.07) отмечена `DONE`, отправлено уведомление
  через `/api/mark-bot/send`.

**10.07.2026** — 4 бага боевой механики карточной игры (по заметкам Марка в тг-боте, 09.07):
- ✅ **Урон не совпадал с силой удара на карте** — бейдж атаки на столе
  показывал базовое значение карты, а реальный урон в бою уже включал
  бонус ауры союзников (`buff_allies`/`acid_blood_buff`/`stealth_buff`,
  напр. Преторианец/Волк дают +1 к атаке своим). Фронт теперь считает и
  показывает ту же ауру, что и бэкенд (`auraAttackBonus` продублирован в
  `src/utils/cardArt.js`, подсветка золотым, если карта усилена).
- ✅ **Болезнь призыва** — карта, только что выложенная на стол (своя или
  боссовская), не может атаковать в этот же ход. Новое поле
  `BoardCreature.summonedTurn` в `cards.routes.ts`; для босса решено проще —
  фаза автобоя в `end-turn` теперь идёт ДО добора/розыгрыша новой карты
  боссом (а не после), так что свежая карта автоматически не успевает
  атаковать в тот же вызов.
- ✅ **Босс держит карту в руке** вместо мгновенного сброса при нехватке
  маны — новое поле `Battle.bossHand` (миграция через `prisma db push` при
  старте Render, как и раньше для остальных Json-полей), симметрично руке
  игрока. Экран боя показывает счётчик карт в руке босса (`BossBanner`).
- ✅ **Нельзя было атаковать карту противника с невидимостью** (Разведчик,
  Сокольник) — баг, а не «непонятная механика»: `stealthCharge` выставлялся
  при создании существа и никогда не сбрасывался при ручной атаке игрока
  (`POST /battle/:id/attack` просто отклонял такую цель ошибкой 400) — карта
  оставалась неуязвимой навсегда, хотя по тексту эффекта невидимость должна
  длиться один ход. Теперь первая атака по невидимой цели — это промах
  (уклонение, тратит заряд), фронт больше не отключает `Pressable` для таких
  карт целиком.
- Все 4 фикса проверены на проде прямыми запросами к API (без браузера —
  в этой сессии не было доступного инструмента для вождения браузера).
  Бэкенд задеплоен на Render (автодеплой), фронтенд — вручную на Cloudflare
  (`npm run deploy`). Заметки Марка в тг-боте отмечены `DONE`, отправлена
  сводка через `/api/mark-bot/send`.
- Не тронуто в этой сессии (отдельные будущие задачи из той же пачки
  заметок 09.07): подсветка активной карты в ход (сейчас все карты/босс
  зеленеют — просили оставить цвет редкости и просто добавить лёгкое
  свечение), вкладка «Игра» в админке Марка, увеличение карты по
  долгому нажатию, отчёт за 30 дней по игре, активируемые способности
  карт (аура-бафы всплывающей циферкой, невидимость по кнопке вместо
  автомата) — Марк также описал это как отдельную более крупную задачу.

**10.07.2026 (2)** — визуал боя + активируемые способности (продолжение той же сессии):
- ✅ **Подсветка активной карты** — раньше карта, которой можно ходить,
  красилась в зелёный (стирая цвет редкости, все карты и босс выглядели
  одинаково). Теперь рамка остаётся цветом редкости всегда (золото — только
  у явно выбранного атакующего), а «можно ходить» показывается лёгким
  пульсирующим ореолом того же цвета (`BoardSlot.js`, `Animated.loop`).
- ✅ **Landscape-lock в бою восстановлен** — обнаружилось, что 02.07
  (коммит `1a6dbcf`, отдельная сессия) принудительный поворот в ландшафт
  для `BattleScreen` убрали в пользу свободного поворота везде (ради
  обязательной геолокации). Если у пользователя выключен авто-поворот
  экрана — игра без явного `ScreenOrientation.lockAsync` физически не
  переходит в ландшафт. Жалоба Марка от 01.07 была ДО этого коммита.
  По решению пользователя — вернули `lockAsync(LANDSCAPE)` только для
  `BattleScreen`, остальные экраны остались со свободным поворотом.
- ✅ **Лупа карты по долгому нажатию** (`CardZoomModal.js`) — увеличенный
  арт + полное описание эффекта (`effectText` не помещается на маленьком
  бейдже руки/стола). Работает в руке и на столе босса всегда; на СВОЕЙ
  карте на столе — только когда она не под управлением жеста атаки
  (`!canSelect`), иначе `Pressable` внутри `GestureDetector` конфликтует
  с перехватом указателя на вебе (та же причина, по которой `onPress`
  туда тоже не передаётся, см. коммит `f4315a0`).
- ✅ **Активируемая невидимость** — по фидбэку Марка карты с эффектом
  stealth/stealth_buff (Разведчик, Сокольник, Волк) должны становиться
  невидимыми ПО ВЫБОРУ игрока («если не хочет терять карту — жмёт
  кнопку»), а не автоматически с момента выхода на стол. `makeBoardCreature`
  получил параметр `autoStealth` (по умолчанию `false`) — для карт игрока
  (`POST /battle/:id/play`) заряд больше не выдаётся сам; для карт БОССА
  (простой автобот без модели решений) старое авто-поведение сохранено
  через `autoStealth=true`, иначе он бы никогда не пользовался
  способностью. Новый бесплатный эндпоинт `POST /battle/:id/activate`
  (только свои живые существа, ещё не активная способность) — на фронте
  кнопка 👁️ в углу карты на столе (`BattleScreen.js`, сиблинг
  `GestureDetector`, не его потомок — по той же причине, что и лупа).
- ✅ **Всплывающий +N при выходе аура-карты** — `DamagePopup.js` получил
  `positive` (золотой «+N» вместо «−N»), триггерится на всех живых
  существах того же стола в момент выхода buff_allies/acid_blood_buff/
  stealth_buff карты (и для игрока в `onPlayCard`, и для босса в
  `playEvents` на событие `boss_play`) — раньше бонус аур влиял только на
  итоговую цифру силы удара незаметно.
- Собран и отправлен Марку через `/api/mark-bot/send` отчёт за 30 дней по
  игре (основа/визуал/онбординг/что стабильно/что отложено) — заранее
  согласован с пользователем перед отправкой.
- Найдено, но НЕ исправлено (отдельная возможная задача на будущее):
  ауры усиления (buff_allies и т.п.) в коде применяются ко ВСЕМ картам на
  столе владельца, без проверки фракции — при этом текст эффекта
  Преторианца говорит «усиливает всех Чужих», Волка — «усиливает всех
  Хищников». У `Card` в схеме нет поля фракции вообще (только по названию
  карты можно понять, кто есть кто) — если колода игрока смешанная
  (Чужие + Хищники), баф в реальности действует на ВСЕХ, а не только на
  свою фракцию, как заявлено в тексте. Не тронуто в этой сессии — требует
  отдельного решения (новое поле `faction` в `Card` + миграция + бэкфилл
  21 карты), не входило в текущий запрос.
- Остались нетронутыми из того же бэклога: вкладка «Игра» в админке
  Марка, увеличение колоды карт, «карта не активна, нет изображения»
  (`cmr4286il`, нужно уточнение у Марка — какая карта), доступ Марка к
  управлению магазином (сознательно отложено, зона Кирилла).

**08.07.2026 (2)** — фикс мобильного браузера + подсказки в игре:
- ✅ Нижний таб-бар перекрывался тулбаром мобильного браузера (Safari/
  Telegram in-app на iPhone) — репортнул пользователь со скриншотом.
  Причина: `html/body{height:100%}` на мобильном резолвится в «большой»
  viewport без учёта показанного тулбара браузера. Фикс — `public/index.html`
  (Expo Metro web подхватывает вместо встроенного шаблона; ВАЖНО — путь
  именно `public/`, не `web/`, хотя `npx expo customize` предлагает
  `web/index.html`) с `100dvh` (фолбэк на `100%`) и `viewport-fit=cover` в
  meta viewport — заодно чинит `env(safe-area-inset-bottom)`, который уже
  использует `useSafeAreaInsets()` в `App.js`, но раньше не получал данных
  в браузере.
- ✅ Кнопка «❔ Как играть» на GameScreen — модалка с правилами (цель,
  мана/здоровье/атака, розыгрыш карт, атака, конец хода), переиспользует
  `ManaBadge`/`HealthBadge`/`AttackBadge` из редизайна иконок этой же сессии
  (`src/utils/HowToPlayModal.js`).
- Оба фикса проверены в браузере (Edge/Playwright) и задеплоены одним
  деплоем на Cloudflare.

**08.07.2026** — восстановление пароля через email:
- ✅ Бэкенд: `POST /api/auth/forgot-password` + `POST /api/auth/reset-password`
  (`collectors-realm-backend/src/routes/auth.routes.ts`,
  `src/services/auth.service.ts`) — по аналогии с уже существующим
  send-code/verify-code для регистрации: 6-значный код на почту через Resend
  (общая `sendCodeEmail`), TTL 5 минут, in-memory Map. Ответ forgot-password
  одинаковый независимо от того, существует ли аккаунт (защита от email-
  enumeration). Добавлен `RESEND_API_KEY` в `.env.example` (был пропущен).
- ✅ Фронтенд: ссылка «Забыли пароль?» на LoginScreen → email → код +
  новый пароль, в стиле уже существующего флоу подтверждения email при
  регистрации. Проверено полностью в браузере (запрос кода, экран ввода,
  обработка неверного кода) — работает без ошибок.
- Оба сервиса задеплоены (Render автодеплой бэкенда, `npm run deploy`
  фронтенда на Cloudflare).

**07.07.2026** — задачи из телеграм-бота Марка (`@collectors_realm_tasks_bot`):
- ✅ Редизайн иконок характеристик карты: жизни — зелёный щит, атака — красный
  клинок-ромб, мана — синий круг с магическим свечением/ореолом. Общие
  компоненты `ManaBadge`/`HealthBadge`/`AttackBadge` в `src/utils/cardArt.js`
  (раньше разметка бейджей дублировалась в HandCard/BoardSlot/GameScreen),
  применены во всех трёх местах. Добавлена зависимость `react-native-svg`
  (`expo install`). Проверено в браузере (коллекция, рука, арена боя) —
  рендерится без ошибок.
- ⏳ Отложено по решению Кирилла: доступ Марка к управлению лотами в магазине
  (сейчас `MODERATOR` не видит вкладку «Товары» — осознанное ограничение)
  и «карта не активна, нет изображения» — нужно уточнить у Марка, какая карта.
- ⏳ Отложена полностью: механика колоды (расширение 10→20, заработок карт за
  игровые действия, объединение 3 одинаковых карт в более редкую с улучшенными
  характеристиками) — сам Марк просил проработать её отдельно, детали
  (какие действия дают карты, шансы редкости, бонусы улучшения) не определены.
- Поворот экрана в игре (жалоба Марка от 01.07) — похоже, уже был исправлен
  коммитами 74d4715/1a6dbcf от 02.07, до подтверждения не проверялось повторно.

## Арт карт игры «Карты Средиземья» (нейрогенерация)

Тема пилот — «Чужой против Хищника», 20 карт (10 Чужие + 10 Хищники) + босс
«Королева чужих» + рубашка колоды. Карты создаются и заливаются вручную по одной.

**Инструмент:** Leonardo.ai, модель **Lucid Origin**, режим **Fast**, стиль
**Dynamic**, размер строго **1:1 (квадрат)** — карты везде отображаются как
квадратные превью с обрезкой по центру (`resizeMode: cover` в `cardArt.js`),
другое соотношение сторон обрежет голову/ноги персонажа.

**Стиль промта:** кинематографичный тёмный постер фильма Alien/Predator,
photorealistic, hyper-detailed, без текста/водяных знаков. Шаблон:
`Cinematic dark sci-fi horror movie poster style, <описание существа/сцены>,
photorealistic, hyper-detailed, square 1:1 composition, dramatic <тип света>,
no text, no watermark`.

**Порядок генерации — от редких к простым** (GOLD → SILVER → EPIC → COMMON),
сначала самые ценные/заметные карты. Всего 20 карт: по 6 COMMON / 2 EPIC /
1 SILVER / 1 GOLD на каждую из двух фракций (Чужие/Хищники), см. `seed-cards.ts`
для точной редкости каждой карты.

- ✅ GOLD (2/2): Предалиен, Волк
- ✅ SILVER (2/2): Преторианец, Старейшина
- ✅ EPIC (4/4): Опустошитель, Городской охотник, Дробитель, Супер-хищник
- ✅ COMMON (12/12): все готовы и залиты, включая Берсерк и Сокольник (01.07.2026)

**Все 20 карт + босс «Королева чужих» + рубашка колоды имеют арт** (последние —
Берсерк и Сокольник — залиты 01.07.2026, `prisma/set-card-images-2026-07-01.sql`,
выполнить в Supabase SQL Editor, если ещё не выполнен). Более нет карт без арта
в теме «Чужой против Хищника».

**Пайплайн загрузки:** файл из `art-cards/<имя-транслит>.png` → unsigned upload
на Cloudinary (`api.cloudinary.com/v1_1/dqutmb1rm/image/upload`, preset
`collectors_realm`) → SQL `prisma/set-card-images-<дата>.sql` с
`UPDATE "Card" SET "imageUrl" = '...' WHERE name = '...'` → выполнить в
Supabase SQL Editor. Папка `art-cards/` в `.gitignore` — исходники не коммитятся,
итоговые URL уже в БД.

## Ключевые зависимости

```json
"expo": "~54.0.33",
"react-native": "0.76.7",
"@react-navigation/native": "^6.x",
"expo-image-picker": "~55.0.14",
"expo-location": "~18.0.10",
"expo-notifications": "~0.32.16",
"react-native-webview": "13.16.1",
"react-native-maps": "^1.x",
"react-leaflet": "^4.x",
"axios": "^1.x"
```
