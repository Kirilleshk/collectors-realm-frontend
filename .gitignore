# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Local Netlify folder
.netlify

# исходники нейроартов карт — уже загружены на Cloudinary, URL в БД
/art-cards

# скриншоты отладки Playwright MCP
/.playwright-mcp

# приватные бизнес-документы (стратегия, планы для партнёров) — репозиторий
# публичный на GitHub, такому здесь не место
/private-docs

# личные разрешения Claude Code для этой машины — копит историю одобренных
# команд, а в неё попадают секреты (BOT_SECRET, пароли, токены) прямо в
# тексте команды; репозиторий публичный, коммитить такое нельзя (см. чистку
# 22.09.2026 — 60 записей с секретами накопились здесь за месяцы сессий)
.claude/settings.local.json
